DATABASE_URL="postgresql://postgres:postgres@localhost:5432/bhishibook?schema=public"
NEXT_PUBLIC_APP_NAME="BhishiBook"
# Signs the session cookie. Generate your own with:
#   node -e "console.log(require('node:crypto').randomBytes(32).toString('base64'))"
SESSION_SECRET="replace-me-with-a-32-byte-random-string"
